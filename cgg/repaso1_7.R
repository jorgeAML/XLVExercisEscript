# Formule cada una de las siguientes expresiones
# como un número racional con el numerador postivo
# más pequeño posible
a1 <- 2/3
a2 <- (-5/8)

if (a1 <= a2) {
  sprintf("El primero es menor: %i", a1)
  } else {
    print("El segudo es mayor")
}
# Encontrar si es menor, mayor o igual
b1 <- (-0.1)
b2 <- (-0.01)
if ( b1 < b2 ) {
  sprintf ("El primero es menor: %f", b1)
  } else {
  print ("El segundo es mayor")
  }
